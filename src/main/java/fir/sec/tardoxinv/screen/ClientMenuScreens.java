package fir.sec.tardoxinv.screen;

/**
 * Deprecated placeholder.
 * Screen registration is handled elsewhere (e.g., ClientModEvents / RegisterMenuScreensEvent).
 * This class intentionally does nothing to avoid duplicate MenuScreens.register(...) calls.
 */
final class ClientMenuScreens {
    private ClientMenuScreens() {}
}
